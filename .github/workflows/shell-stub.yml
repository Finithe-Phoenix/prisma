name: shell (Rust) — stub

# Rust shell validation. This temporary diagnostic branch adds one isolated,
# read-only ARM64 job against PR #312; it will be closed without merging.

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: shell-${{ github.ref }}
  cancel-in-progress: true

jobs:
  shell-check:
    name: shell-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Verify shell/ scaffolding
        run: |
          set -euo pipefail
          test -f shell/README.md || { echo "shell/README.md missing"; exit 1; }
          if [ -f shell/Cargo.toml ]; then
            echo "shell/Cargo.toml detected — running real build"
            rustup toolchain install stable --profile minimal --component clippy --component rustfmt
            cargo fmt --manifest-path shell/Cargo.toml --all -- --check
            cargo clippy --manifest-path shell/Cargo.toml -- -D warnings
            cargo test --manifest-path shell/Cargo.toml
          else
            echo "shell/Cargo.toml not yet present (Fase 3 not started). Scaffolding check only."
          fi

  pr312-adc-arm64-diagnostic:
    name: pr312-adc-arm64-diagnostic
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 35
    steps:
      - name: Checkout diagnostic controls
        uses: actions/checkout@v7
        with:
          path: diagnostic

      - name: Checkout PR 312 product branch
        uses: actions/checkout@v7
        with:
          ref: claude/w2-flags-isa-batch
          fetch-depth: 0
          path: product

      - name: Instrument product checkout only
        run: python3 diagnostic/tools/ci/instrument_pr312_adc.py product

      - name: Capture source locations
        shell: bash
        run: |
          set -euo pipefail
          {
            grep -n -E 'fn value_reg|fn lower_binop|LoadCarry|ReadCarryOut|StoreCarry' \
              product/shell/prisma-backend/src/lowerer.rs
            grep -n -E 'Adc|Sbb|LoadCarry|ReadCarryOut|StoreCarry' \
              product/shell/prisma-decoder/src/decode.rs
          } > adc-source-locations.log || true

      - name: Install Rust toolchain
        run: rustup toolchain install stable --profile minimal

      - name: Run targeted ADC/SBB tests with full output
        shell: bash
        env:
          RUST_BACKTRACE: "1"
        run: |
          set -o pipefail
          cargo test --manifest-path product/shell/Cargo.toml \
            -p prisma-runtime --test exec_adc -- --nocapture \
            2>&1 | tee adc-test.log

      - name: Upload ADC/SBB diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr312-adc-arm64-diagnostics
          retention-days: 2
          if-no-files-found: error
          path: |
            adc-test.log
            adc-source-locations.log
